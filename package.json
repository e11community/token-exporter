{
  "name": "@e11community/token-exporter",
  "version": "2.0.2",
  "description": "Find NPM auth tokens in HOME/.npmrc and exports them as environments variables",
  "main": "lib.js",
  "bin": {
    "export-tokens": "./export-tokens.js"
  },
  "scripts": {
    "atest": "jest ${0}",
    "build": "tsc",
    "build:watch": "tsc --watch",
    "clean": "gts clean",
    "fix": "gts fix",
    "lint": "gts lint",
    "publish": "yarn npm publish",
    "test": "jest",
    "test:coverage": "jest --coverage",
    "version": "yarn version",
    "prepack": "yarn run clean && yarn run build",
    "pretest": "yarn run build",
    "posttest": "yarn run lint"
  },
  "keywords": [
    "auth",
    "token"
  ],
  "author": "Engineering 11",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/e11community/token-exporter/issues"
  },
  "repository": {
    "url": "https://github.com/e11community/token-exporter",
    "type": "git"
  },
  "publishConfig": {
    "registry": "https://npm.pkg.github.com/e11community"
  },
  "homepage": "https://github.com/e11community/token-exporter#readme",
  "devDependencies": {
    "gts": "^3.1.0",
    "ts-node": "^10.1.0"
  },
  "dependencies": {
    "@types/node": "^16.4.10",
    "typescript": "^4.3.5"
  }
}
